spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/academiaDio?useTimezone=true&serverTimezone=UTC
    username: pedro
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


